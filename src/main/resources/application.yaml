spring:
  jpa:
    open-in-view: false
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

management:
  endpoints:
    web:
      exposure:
        include: "*"

cache:
  properties:
    expires-after: 1
    time-unit: DAYS